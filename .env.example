# SvelteKit frontend — Django API base URL for server-side public data loads
# Local:
PUBLIC_DJANGO_API_BASE_URL=http://127.0.0.1:8000
# Vercel production (set in Vercel dashboard, not committed):
# PUBLIC_DJANGO_API_BASE_URL=https://api.yourdomain.org
